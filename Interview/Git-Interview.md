## git blame

面试题: 如何查看某行代码是谁写的？

最常用的命令是 `git blame`。

在简单场景下，我直接执行 `git blame <文件名>` 就能看到每行代码的作者和提交 ID。

在复杂排查中:

+ 用 `-L` 指定行号范围来缩小搜索

+ 用 `-w` 忽略掉那些仅仅是因为代码格式化导致的干扰
+ `-M`: 识别**同一个文件中代码位置发生变化**
+ `-C`: 追踪代码是否**从其他文件复制过来**, 能追溯到**原始文件和作者**